import {useState} from "react";

function home() {
  return (
    <header>
      <div className="company">Hostel Stays</div>
        <button>Login</button>
        <button>Signup</button>
    </header>
  );
}

export default home;
