import React from "react";

const Home = () => {
  return (
    <header>
      <div className="company">Hostel Stays</div>
      <button>Login</button>
      <button>Signup</button>
    </header>
  );
};

export default Home;
